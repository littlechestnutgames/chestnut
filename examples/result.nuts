let res = 0
fn add_one(val : Integer) returns Result
    return new_result(val + 1, null)
endfn

let initial_result = new_result(5, null)

let final_result = initial_result
    .and_then(add_one)
    .and_then(add_one)

# Expected Output: The number 7
print("Final value: {{ final_result.value }}")

# Expected Output: null
print("Error value: {{ final_result.error }}")
